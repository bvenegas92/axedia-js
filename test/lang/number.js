/*describe("Number", function() {
    var num;

    beforeEach(function() {
        num = 8;
    });

    it('constrain[5, 10]', function() {
        var newNum = Number.constrain(num, 5, 10);

        expect(newNum).toBeGreaterThan(4);
        expect(newNum).toBeLessThan(11);
    });

    it('correctFloat', function() {
        var correctFloat = Number.correctFloat(0.1 + 0.2);

        expect(correctFloat).toEqual(0.3);
    });

    it('decimalAdjust', function() {
        var newNum = Number.decimalAdjust(123.456789, 3);

        expect(newNum).toBeGreaterThan(123);
    });

    it('round', function() {
        var newNum = Number.round(123.456789, 3);

        expect(newNum).toBe(123.457);
    });

    it('truncate', function() {
        var newNum = Number.truncate(123.456789, 3);

        expect(newNum).toBe(123.456);
    });

    it('ceil', function() {
        var newNum = Number.ceil(123.456789, 3);

        expect(newNum).toBe(123.457);
    });

    it('randomInt', function() {
        var newNum = Number.randomInt(5, 10);

        expect(newNum).toBeGreaterThan(4);
    });

    it('randomFloat', function() {
        var newNum = Number.randomFloat(0.01, 0.5);

        expect(newNum).toBeGreaterThan(0.01);
    });
});
*/
