/*describe("Convert", function() {

    if (Convert.decToHex) {
        it('decToHex', function() {
            var value = Convert.decToHex(255);

            expect(value).toEqual("ff");
        });
    }

    if (Convert.hexToDec) {
        it('hexToDec', function() {
            var value = Convert.hexToDec("ff");

            expect(value).toEqual(255);
        });
    }

    if (Convert.HexToHSL) {
        it('HexToHSL', function() {
            var value = Convert.HexToHSL("#64C8FA");

            expect(value[0]).toBeCloseTo(200, 201);
            expect(value[1]).toBeCloseTo(0.93, 0.94);
            expect(value[2]).toBeCloseTo(0.68, 0.69);
        });
    }

    if (Convert.HexToRGB) {
        it('HexToRGB', function() {
            var value = Convert.HexToRGB("#64C8FA");

            expect(value).toEqual([100, 200, 250]);
        });
    }

    if (Convert.HSLToHex) {
        it('HSLToHex', function() {
            var value = Convert.HSLToHex(200, 0.933, 0.688);

            expect(value).toEqual("#65C8FA");
        });
    }

    if (Convert.HSLToRGB) {
        it('HSLToRGB', function() {
            var value = Convert.HSLToRGB(200, 0.933, 0.688);

            expect(value[0]).toBeGreaterThan(99);
            expect(value[1]).toBeGreaterThan(199);
            expect(value[2]).toBeGreaterThan(249);
        });
    }

    if (Convert.RGBToHex) {
        it('RGBToHex', function() {
            var value = Convert.RGBToHex(100, 200, 250);

            expect(value).toEqual("#64C8FA");
        });
    }

    if (Convert.RGBToHSL) {
        it('RGBToHSL', function() {
            var value = Convert.RGBToHSL(100, 200, 250);

            expect(value[0]).toBeCloseTo(200, 201);
            expect(value[1]).toBeCloseTo(0.93, 0.94);
            expect(value[2]).toBeCloseTo(0.68, 0.69);
        });
    }
});
*/
