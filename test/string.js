describe("String", function() {
    if (Axedia.String) {
        /*if (Axedia.String.capitalize) {
            it('capitalize debe convertir a mayuscula', function() {
                var capitalize = Axedia.String.capitalize('alfa');

                expect(capitalize).toEqual('Alfa');
            });
        }

        if (Axedia.String.ellipsis) {
            it('ellipsis debe truncar', function() {
                var ellipsis = Axedia.String.ellipsis('alfa, beta, gamma & delta', 10, true);

                expect(ellipsis).toEqual('alfa,...');
            });
        }

        if (Axedia.String.endsWith) {
            it('endsWith debe verificar al final', function() {
                var endsWith = Axedia.String.endsWith('alfa, beta, gamma & delta', 'delta');

                expect(endsWith).toBe(true);
            });
        }

        if (Axedia.String.insert) {
            it('insert debe insertar', function() {
                var insert = Axedia.String.insert('alfa', '-', 2);

                expect(insert).toBe('al-fa');
            });
        }

        if (Axedia.String.leftPad) {
            it('leftPad debe rellenar por la izquierda', function() {
                var leftPad = Axedia.String.leftPad('alfa', 10, '-');

                expect(leftPad).toBe('------alfa');
            });
        }

        if (Axedia.String.random) {
            it('random debe generar un string que haga match con [a-zA-Z0-9]{1, 32}', function() {
                var string = Axedia.String.random();

                expect(string).toMatch(/[a-zA-Z0-9]{1,32}/);
            });
        }

        if (Axedia.String.repeat) {
            it('repeat debe repetir', function() {
                var repeat = Axedia.String.repeat('alfa', 3, '-');

                expect(repeat).toBe('alfa-alfa-alfa');
            });
        }

        if (Axedia.String.splitWords) {
            it('splitWords debe separar palabras', function() {
                var words = Axedia.String.splitWords('alfa beta  gamma   delta');

                expect(words).toEqual(['alfa', 'beta', 'gamma', 'delta']);
            });
        }

        if (Axedia.String.startsWith) {
            it('startsWith debe verificar al principio', function() {
                var startsWith = Axedia.String.startsWith('alfa, beta, gamma & delta', 'alfa');

                expect(startsWith).toBe(true);
            });
        }

        if (Axedia.String.toggle) {
            it('toggle debe alternar', function() {
                var toggle = Axedia.String.toggle('alfa', 'beta', 'alfa');

                expect(toggle).toEqual('beta');
            });
        }

        if (Axedia.String.trim) {
            it('trim debe eliminar espacions', function() {
                var trim = Axedia.String.trim('   alfa   ');

                expect(trim).toEqual('alfa');
            });
        }

        if (Axedia.String.uncapitalize) {
            it('uncapitalize debe eliminar espacions', function() {
                var uncapitalize = Axedia.String.uncapitalize('Alfa');

                expect(uncapitalize).toEqual('alfa');
            });
        }*/
    }
});
