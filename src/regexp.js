define(function() {
    // RegExp namespace
    var $RegExp = {
        /**
         * Espacios (i.e. ' ', \t, \n, \r, \v) al inicio y fin
         *
         * @type {RegExp}
         */
        BASIC_TRIM: /^\s+|\s+$/g,
        WHITE_SPACE: /\s+/
    };
});
