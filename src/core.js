define(function() {
    // variables / funciones cacheadas
    var mathFloor = Math.floor,
    mathRandom = Math.random,
    arrayPrototype = Array.prototype,
    objectPrototype = Object.prototype,
    slice = arrayPrototype.slice,

    // Namespaces
    $Array,
    $Number,
    $RegExp,
    $String;
});
